import React from 'react';
import { Text, View } from 'react-native';

export function WorkspaceScreen() {
  return (
    <View>
      <Text style={{ color: '#fff' }}>Workspace mirror view (MVP scaffold)</Text>
    </View>
  );
}
